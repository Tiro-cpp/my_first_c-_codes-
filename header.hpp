#ifndef HEADER_HPP
#define HEADER_HPP
#include <vector>
void swap_with_reference(int& , int&);
void reverse_name ( std::string& );
void calculate_numbers ( std::vector<int>& , int& , int& );
void largest_smallest  ( std::vector<int>& , int& , int& );
#endif 
